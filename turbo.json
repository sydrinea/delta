{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "test": {
      "dependsOn": ["^test", "^generate"]
    },
    "build": {
      "dependsOn": ["^build", "^generate"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "dev": {
      "dependsOn": ["^generate"],
      "persistent": true,
      "cache": false
    },
    "generate": {
      "inputs": ["nfa/**/*.meta.ts", "tm/**/*.meta.ts"],
      "outputs": [
        "generated/index.ts",
        "generated/recipes.ts",
        "../apps/client/public/examples/**"
      ]
    }
  }
}
