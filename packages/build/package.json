{
  "name": "@delta/build",
  "version": "0.3.0-beta.1",
  "private": "true",
  "license": "MIT",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint",
    "generate": "tsdown --dts",
    "test": "vitest run"
  },
  "dependencies": {
    "@delta/shared": "workspace:*",
    "vitest": "^4.1.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "tsdown": "^0.21.7",
    "typescript": "^6.0.2"
  }
}
